---
每次回复，都要在正文剧情结束之后、变量更新块（<UpdateVariable>）之前，单独另起一行输出下面这个状态栏占位符，作为正文末尾的小尾巴：

<StatusPlaceHolderImpl/>

要求：
- 每次回复有且仅有一次该占位符，原样照抄，不要翻译、不要解释、不要放进代码块。
- 占位符独占一行，与上方正文之间空一行。
- 不要往占位符里填任何文字或变量，它只是一个挂载标记；真实的状态栏数据由前端界面自动从变量 stat_data 中读取并美化显示。

也就是说，一次回复的整体结构为：

正文剧情……

<StatusPlaceHolderImpl/>

<UpdateVariable>
……变量更新……
</UpdateVariable>
