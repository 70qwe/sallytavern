<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <% if (typeof BASE_HREF !== 'undefined' && BASE_HREF) { %>
    <base href="<%= BASE_HREF %>" />
    <% } %>
    <title>余烬 · user的末日游戏</title>
  </head>
  <body>
    <div id="root"></div>
  </body>
</html>
